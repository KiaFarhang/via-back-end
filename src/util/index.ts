import {YelpBusiness} from "../types";

export const buildCostString = (stars: string): string => {
    return "hi";
};

export const calculateTimeNeeded = ({categories, rating}: YelpBusiness): number => {
    return 4;
};
